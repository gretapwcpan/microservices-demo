# This is a template. Replace with actual values before deploying.
# For production, use Google Secret Manager or sealed-secrets
apiVersion: v1
kind: Secret
metadata:
  name: gemini-secret
  namespace: quanbuy
type: Opaque
stringData:
  api-key: "YOUR_GEMINI_API_KEY_HERE"  # Replace with actual Gemini API key
---
apiVersion: v1
kind: Secret
metadata:
  name: gcp-service-account-key
  namespace: quanbuy
type: Opaque
data:
  key.json: |
    # Base64 encoded GCP service account JSON key
    # Generate with: cat service-account-key.json | base64 -w 0
    # Replace this comment with the actual base64 encoded key
